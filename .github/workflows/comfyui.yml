name: ROCm Images Build

# Reusable building block invoked by the release orchestrator (release.yml),
# which computes the version ONCE and passes it in. Not triggered on its own —
# the orchestrator owns triggers, per-family gating, concurrency and release.
on:
  workflow_call:
    inputs:
      version:
        description: "Semantic version without a leading v (e.g. 1.1.0)."
        required: true
        type: string
      suffix:
        description: "Tag suffix: empty for releases, -rc.<pr> for PRs, -dev otherwise."
        required: false
        default: ""
        type: string

permissions:
  contents: read

jobs:
  build-comfyui:
    runs-on: blacksmith-8vcpu-ubuntu-2404
    strategy:
      fail-fast: false
      matrix:
        comfyui: [cuda128, cuda130]
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          fetch-depth: 0
      
      - name: Setup Docker
        uses: ./.github/actions/docker-setup
        id: setup
        with:
          dockerhub-username: ${{ secrets.DOCKERHUB_USERNAME }}
          dockerhub-token: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build comfyui images (${{ matrix.comfyui }})
        id: build
        uses: docker/bake-action@6614cfa25eff9a0b2b2697efb0b6159e7680d584
        env:
          BUILDX_BAKE_ENTITLEMENTS_FS: 0
          RELEASE_VERSION: ${{ inputs.version }}
          RELEASE_SUFFIX: ${{ inputs.suffix }}
        with:
          source: .
          files: |
            official-templates/comfyui/docker-bake.hcl
          load: true
          push: false
          targets: |
            ${{ matrix.comfyui }}

      - name: Extract image refs
        id: refs
        uses: ./.github/actions/image-name
        with:
          bake-metadata: ${{ steps.build.outputs.metadata }}

      - name: Grype scan
        uses: ./.github/actions/grype
        with:
          image-refs: ${{ steps.refs.outputs.refs }}

      - name: Push images
        uses: ./.github/actions/docker-push
        with:
          image-refs: ${{ steps.refs.outputs.refs }}
          ref-digests: ${{ steps.refs.outputs.ref-digests }}

      - name: Install Cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6
        with:
          cosign-release: v2.6.0

      - name: Cosign sign and verify
        uses: ./.github/actions/cosign
        with:
          image-digests: ${{ steps.refs.outputs.digests }}

      - name: Save refs artifact
        shell: bash
        env:
          REFS: ${{ steps.refs.outputs.refs }}
        run: |
          mkdir -p /tmp/comfyui-refs
          printf '%s\n' "$REFS" > /tmp/comfyui-refs/refs.json

      - name: Upload refs artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: comfyui-refs-${{ matrix.comfyui }}
          path: /tmp/comfyui-refs/refs.json
          retention-days: 1
          if-no-files-found: error
          
  test-comfyui:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    needs: build-comfyui
    if: needs.build-comfyui.result == 'success'
    strategy:
      fail-fast: false
      matrix:
        comfyui: [cuda128, cuda130]
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          fetch-depth: 0

      - name: Download refs for ${{ matrix.comfyui }}
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: comfyui-refs-${{ matrix.comfyui }}
          path: /tmp/comfyui-refs

      - name: Read refs
        id: refs
        shell: bash
        run: |
          REFS=$(cat /tmp/comfyui-refs/refs.json)
          {
            echo "refs<<EOF"
            echo "$REFS"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Smoke test (${{ matrix.comfyui }})
        uses: ./.github/actions/smoke-test
        with:
          image-refs: ${{ steps.refs.outputs.refs }}
          profile: gpu
          runpod-api-key: ${{ secrets.TESTING_RUNPOD_API_KEY }}
          ssh-private-key: ${{ secrets.TESTING_RUNPOD_SSH_PRIVATE_KEY }}
          on-skip: "warn"
          manufacturer: "NVIDIA"
          budget-usd-per-hour: "3.0"
          min-vram-gb: "80"
          create-timeout: "1200"
